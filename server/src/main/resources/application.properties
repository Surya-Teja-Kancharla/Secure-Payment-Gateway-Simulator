# =========================================================
# 🏦 APPLICATION DETAILS
# =========================================================
spring.application.name=SecurePaymentGateway

# =========================================================
# 🌐 DATABASE CONFIGURATION (PostgreSQL)
# =========================================================
spring.datasource.url=jdbc:postgresql://trolley.proxy.rlwy.net:29374/railway
spring.datasource.username=postgres
spring.datasource.password=wcBdwiiONBdWHJYpQloBZEaeugyVGrrM
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.hikari.data-source-properties.serverTimezone=Asia/Kolkata

# =========================================================
# 🧩 JPA (Hibernate) CONFIGURATION
# =========================================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# ✅ Ensure data.sql runs properly on Railway
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true

# =========================================================
# ⚙️ SERVER CONFIGURATION
# =========================================================
server.port=8080

# =========================================================
# 🧰 OPTIONAL DEVTOOLS SETTINGS (auto reloads for development)
# =========================================================
spring.devtools.restart.enabled=true
spring.devtools.livereload.enabled=true
